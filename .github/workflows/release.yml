name: Release (Unsigned DMG)

# No Apple Developer account: CI builds are ad-hoc signed (run locally after
# right-click -> Open). Mirrors the local Release build: xcodebuild, then a
# plain UDZO DMG (the copy inside is signature-verified before packing).
# No Sparkle in Nightfall — the update checker is removed from this fork,
# releases are manual.
on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: macos-26
    permissions:
      contents: write
    env:
      APP_NAME: Nightfall
      SCHEME: Nightfall
      CONFIGURATION: Release
      PROJECT: Nightfall.xcodeproj
      DERIVED_DATA_PATH: ${{ github.workspace }}/build/DerivedData
      APP_PATH: ${{ github.workspace }}/build/DerivedData/Build/Products/Release/Nightfall.app
      DMG_PATH: ${{ github.workspace }}/build/Nightfall-arm64.dmg
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Xcode version
        run: xcodebuild -version

      - name: Build (ad-hoc, no Developer ID)
        run: |
          xcodebuild \
            -project "$PROJECT" \
            -scheme "$SCHEME" \
            -configuration "$CONFIGURATION" \
            -destination "generic/platform=macOS" \
            -derivedDataPath "$DERIVED_DATA_PATH" \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY=- \
            CODE_SIGNING_REQUIRED=NO \
            PROVISIONING_PROFILE_SPECIFIER="" \
            ARCHS=arm64 \
            ONLY_ACTIVE_ARCH=YES \
            build

      - name: Ad-hoc sign and verify
        run: |
          set -euo pipefail
          codesign --force --sign - --deep "$APP_PATH"
          codesign --verify --strict --verbose=2 "$APP_PATH"

      - name: Create DMG
        run: |
          set -euo pipefail
          rm -rf "${{ github.workspace }}/build/dmg"
          mkdir -p "${{ github.workspace }}/build/dmg"
          ditto "$APP_PATH" "${{ github.workspace }}/build/dmg/${{ env.APP_NAME }}.app"
          # The copy must still pass strict verification before it goes into the DMG.
          codesign --verify --strict "${{ github.workspace }}/build/dmg/${{ env.APP_NAME }}.app"
          ln -s /Applications "${{ github.workspace }}/build/dmg/Applications"
          hdiutil create -volname "$APP_NAME" -srcfolder "${{ github.workspace }}/build/dmg" -ov -format UDZO "$DMG_PATH"

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: Nightfall-arm64-dmg
          path: "${{ env.DMG_PATH }}"

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh release create "${{ github.ref_name }}" "$DMG_PATH" --generate-notes
